{
    "name": "rrhh/sistema-recursos-humanos",
    "description": "Sistema de gestión de recursos humanos con PHP MVC",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "mpdf/mpdf": "^8.2"
    },
    "suggest": {
        "ext-pdo": "Required for database access",
        "ext-pdo_mysql": "Required for MySQL database",
        "ext-mbstring": "Required for MPDF and string operations",
        "ext-gd": "Required for MPDF image processing",
        "ext-json": "Required for JSON operations"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "optimize-autoloader": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
